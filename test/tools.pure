namespace utl;

using system;

// macro to time (short) evaluations
def timex x = y,(t2-t1)/CLOCKS_PER_SEC when
                t1 = clock; 
                y = x; 
                t2 = clock;
              end;
